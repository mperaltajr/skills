# v0.2 Layout Inheritance — Design Lock

**Date:** 2026-05-28
**Status:** `planning-locked`. NOT `audit-confirmed`. Per `feedback_cleanup_chat_cannot_self_declare.md`, this chat cannot self-promote past planning-locked. Implementation must happen in a separate chat; a third fresh-context audit chat verifies the implementation against this spec.
**Scope:** Phase 1 (body-canonical + bespoke classification, chrome inheritance for cover-class layouts, master-chrome flow-through for body-class layouts). Phase 2 scope reduced to a v0.2.x item — see § "Phase 2 deferred."
**Predecessor:** the 6-agent committee analysis in this chat (Agents X/Y/Z on architecture; Agents P/D/R on phase scoping).

---

## 1. The pain this solves

Mario builds decks against client templates (FedEx, NFL, internal Accenture). Current `slide-builder` builds every slide on a blank 1280×720 canvas at hardcoded `twins/helpers.py` positions, then grafts onto the client template's blank layout. Result: every built slide is "slightly off-spec" relative to the client's chrome (title position drift, footer-band collisions, lost gradient frames, lost cover backgrounds). Mario hand-edits every slide to translate.

This design eliminates the hand-editing for **90% of body slides** by inheriting master chrome correctly and keeping Slide Lab's canonical positions consistent across the deck. Cover-class layouts inherit positions from the template exactly.

---

## 2. The architecture decisions (locked)

### 2.1 Two layout classes — body-canonical vs bespoke

| Class | Definition | Position source | Chrome source |
|---|---|---|---|
| **body-canonical** | Blank-ish body + chrome frame; master decorates, layout is mostly empty (FedEx body layout, Accenture body layout, ~90% of consulting body layouts) | **Slide Lab's canonical helpers** (`twins/helpers.py`) | Master flows through via `finalize_deck.py` graft. Layout placeholders stripped via existing `_strip_layout_placeholders`. |
| **bespoke** | Art-directed (cover, section divider, dark hero) — chrome IS the slide, positions matter | **chrome.yml** extracts placeholder positions per layout | Master chrome inherited identically; placeholders at extracted positions |

Classification happens at template registration. Heuristic: layout has ≤2 art-direction shapes outside chrome zones → `body-canonical`; everything else → `bespoke`. **User confirms classification in `register.html` before commit** — never silent.

### 2.2 Don't modify the source `.pptx`

Mario asked whether the template's placeholders could be repositioned to match Slide Lab's canonical positions. **Decision: don't modify the source `.pptx`.** Mario's instinct (keep positions consistent across the deck) is achieved without touching the template by:

- For `body-canonical` layouts: Slide Lab uses canonical positions in `helpers.py`; master chrome flows through unchanged; layout placeholders stripped at graft.
- For `bespoke` layouts: chrome.yml stores extracted positions; helpers use those.

The template stays pristine. If FedEx re-issues `FDX Template.pptx`, `register_template.py` re-extracts chrome.yml; the original .pptx is read-only.

### 2.3 `layout:` field in brief — fail-loud, with deck-level escape hatch

Briefs declare which layout each slide uses. Three valid forms:

1. **Deck-level default** in front-matter (most common):
   ```yaml
   ---
   client_template: C:\Users\...\FDX Template.pptx
   deck_type: Recommendation / POV
   default_layout: body_canonical_light    # applies to every slide unless overridden
   ---
   ```
2. **Per-slide override** in the slide block:
   ```markdown
   ### Slide 4 — Mid-deck emphasis
   **Layout:** body_canonical_dark
   ```
3. **Cover slide override:**
   ```markdown
   ### Slide 1 — Cover
   **Layout:** cover_light
   ```

**Fail-loud rule:** `build_deck.py` exits at startup with exit code 9 if any slide has no resolvable layout (neither deck default nor per-slide). Error message enumerates the offending slides AND the available layouts from `<stem>.layouts.yml`. **No silent defaults at any level.** See § 5.1 for exact error format.

### 2.4 Bottom-anchor invariant wins over client placeholder anchor

`feedback_title_bottom_anchor.md` says title bottom-y is fixed; 2-line titles grow upward; subtitle never displaces. Client title placeholders are usually top-anchored (PowerPoint default). If we inherit anchor, 2-line titles push subtitle down, breaking the cross-slide visual stability the invariant exists to preserve.

**Decision:** chrome.yml inherits the placeholder's bounding box (x/y/w/h) but Slide Lab keeps `anchor="bottom"` hardcoded in helpers. The placeholder defines WHERE the title region lives; Slide Lab decides HOW text fills that region.

### 2.5 min_body_zone — hard SKELETON_REJECTED (defensive)

When the picked layout's body_zone is smaller than what a pattern needs, the worker emits `# SKELETON_REJECTED: pattern <N> requires <wxh>, layout <name> provides <wxh>` instead of warn-and-scale. Geometric version of Hardline #5.

This won't fire for FedEx-style templates (large body zones). It's a defensive guardrail for future cramped templates. Land with code; ignore if it never fires.

### 2.6 Dark variants via `text_role` field

Each layout in chrome.yml carries:
- `text_role: "dark_on_light" | "light_on_dark"`
- `background: "light" | "dark"` (informational, drives master classification at finalize)

Helpers (`add_title_block`, `add_footer`, body composers) read `text_role` and select text color:
- `dark_on_light` → `TEXT_DARK` / `TEXT_FAINT_ON_LIGHT`
- `light_on_dark` → `WHITE` / `TEXT_FAINT_ON_DARK`

A FedEx-style template with `body_canonical_dark` layout: Slide Lab builds body content at canonical positions; helpers flip text colors via `text_role`; master fills the canvas dark. Mid-deck emphasis slides become structural, not per-pattern hacks.

Preview file showing this end-state: `C:\Users\m.a.peralta\Documents\slide-lab-dark-variant-preview-2026-05-28.pptx`.

---

## 3. Phase 1 commits

Each step is one logical commit. Each commit independently passes `_contract.py` and leaves the system usable.

### P1.1 — `_chrome_schema.py` + manifest entry

**New file:** `slide-builder/scripts/_chrome_schema.py`
**Pattern:** Mirror `_meta_schema.py` shape. Pydantic at `schema_version: int = 1`.

```python
class BoxPx(BaseModel):
    x_px: int
    y_px: int
    w_px: int
    h_px: int
    font_pt: int | None = None
    anchor: Literal["top", "middle", "bottom"] = "top"

class LayoutChrome(BaseModel):
    name: str                                       # layout name from template
    layout_class: Literal["body-canonical", "bespoke"]
    text_role: Literal["dark_on_light", "light_on_dark"]
    background: Literal["light", "dark"]
    has_page_number: bool
    # For bespoke only — None for body-canonical:
    title: BoxPx | None = None
    subtitle: BoxPx | None = None
    footnote: BoxPx | None = None
    source: BoxPx | None = None
    page_number: BoxPx | None = None

class ChromeSpec(BaseModel):
    schema_version: int = 1
    source_template_sha8: str
    layouts: dict[str, LayoutChrome]
```

**Edit:** `slide-builder/scripts/_paths.py` ~line 40 — add `chrome_yml(template_pptx: Path) -> Path` helper + `ARTIFACT_MANIFEST` entry `{"name": "chrome_yml", "writer": "register_template.py", "readers": ["finalize_deck.py", "twins/helpers.py", "build_deck.py"]}`.

**Reversible.**

### P1.2 — Extractor + register.html classification UI

**Edit:** `slide-builder/scripts/register_template.py` — add `extract_chrome_spec(prs) -> ChromeSpec` near the existing brand-extraction code. For each `slide_layout`:

1. Classify as `body-canonical` or `bespoke` (heuristic: count non-chrome art-direction shapes; user can override).
2. For `bespoke` layouts: walk placeholders, extract bounds in EMU, convert to px (96 DPI), populate the `title/subtitle/footnote/source/page_number` fields. If any expected placeholder is absent → the field stays `None`. **No silent fallback to canonical positions.**
3. For `body-canonical` layouts: leave position fields `None`; only fill `layout_class`, `text_role`, `background`, `has_page_number`.
4. Detect `text_role` via master background luminance + default text-color theme reference.

**Edit:** `register.html` template — add a "Layouts" section that lists every layout with its proposed classification + a "use this layout" / "skip this layout" / "reclassify" toggle. User commits per-layout. Mirrors the existing brand-color picker pattern.

**Edit:** `cmd_commit` — after `write_brand_yml(...)`, call `write_chrome_yml(chrome_yml_path, extract_chrome_spec(prs))`. Stamp `source_template_sha8 = sha8(template_pptx)`.

**Reversible** (chrome.yml is additive; no reader consumes it yet).

### P1.3 — Reader + helpers switch

**Edit:** `slide-builder/twins/helpers.py`:

- `add_title_block()` and `add_footer()` accept `chrome: LayoutChrome | None = None`.
- If `chrome is None` AND we're not in a tested-allowed context → raise `ChromeSidecarMissingError`. **No silent fallback to hardcoded constants.**
- If `chrome.layout_class == "body-canonical"` → use canonical positions (today's hardcoded values stay, but only because the helper chose them deliberately based on layout class — not because the sidecar was missing).
- If `chrome.layout_class == "bespoke"` → use the position fields from chrome. Raise if any expected field is `None`.
- Text colors derived from `chrome.text_role`.

**Edit:** `slide-builder/scripts/finalize_deck.py`:

- Load `<stem>.chrome.yml` before exec'ing option scripts. Inject as `twins.helpers._ACTIVE_CHROME`.
- Validate `chrome.source_template_sha8 == sha8(template)` before proceeding. **Hard fail if mismatch.**
- Graft each slide onto the layout named in `_meta.json[slides][i].layout`, not always blank. Replace the `_find_blank_layout(prs)` call in the per-slide loop with `_find_named_layout(prs, slide.layout) or _find_blank_layout(prs)`.

**Forward-only.** Helpers + finalize coupled. Rollback requires reverting both.

### P1.4 — `layout:` field parsing + meta.json

**Edit:** `slide-builder/scripts/build_deck.py`:

- Parse `**Layout:**` per slide.
- Parse `default_layout:` from front-matter.
- For each slide: layout = per-slide `**Layout:**` OR `default_layout` OR fail-loud with exit code 9 + error message enumerating offending slides + available layouts.

**Edit:** `slide-builder/scripts/_meta_schema.py`:

- Bump `META_SCHEMA_VERSION_CURRENT` from 2 to 3.
- Add `layout: str` (required, non-empty) to per-slide dict.
- Add `SUPPORTED_SCHEMA_VERSIONS = (2, 3)` compat shim: v2 meta files still load via a fallback that injects `layout = "body_canonical_light"` and emits a `migration-required` warning to RESULT.md.

**Forward-only.** Schema bump.

### P1.5 — 5 contract checks (land with P1.3 + P1.4)

**Edit:** `slide-builder/scripts/_contract.py`. Add five new checks per Agent D:

| Check function | Asserts |
|---|---|
| `check_chrome_schema_roundtrip()` | Synthetic `ChromeSpec` serializes → dict → revalidates without drift. Schema version preserved. |
| `check_chrome_sidecar_no_silent_fallback()` | Mock-patches chrome.yml away from a registered template; calls `add_title_block(chrome=None)`; asserts it raises `ChromeSidecarMissingError`. **Catches the v1 slot-mapping silent-fallback bug class.** |
| `check_sidecar_freshness_vs_template_sha()` | Builds a registered template, mutates the .pptx bytes by 1, runs `finalize_deck.py`; asserts hard fail with `source_template_sha8 mismatch`. |
| `check_chrome_field_single_source()` | Greps `twins/helpers.py`, `slide-qc/`, and every pattern helper for numeric coordinate literals that look like chrome positions (x in {58, 64, 1170}; y in {20, 100, 660, 672, 676, 688}). Allowlist exactly one location: chrome.yml/`_chrome_schema.py`. Any other location is a contract violation. Modeled on `check_handoff_coverage`'s grep approach. |
| `check_layout_inheritance_roundtrip_per_layout()` | For every registered template, re-extract chrome.yml from the .pptx, compare every layout's placeholder bounds to the stored sidecar within 1 EMU. **Per-layout, not per-template** (per Agent D's prediction — catches the "extraction picked wrong layout's footer" failure). |

### P1.6 — Memory files

**New:** `C:\Users\m.a.peralta\.claude\projects\C--Users-m-a-peralta--claude-skills\memory\feedback_sidecar_fallback_must_be_loud.md`

Content: "Any sidecar/config file whose absence is recoverable must be recovered loudly. Silent fallback to hardcoded defaults is the v1 slot-mapping bug class. If the sidecar is missing or any required field is null, raise; do not substitute. See `_chrome_schema.py::ChromeSidecarMissingError` for the canonical pattern."

**New:** `C:\Users\m.a.peralta\.claude\projects\C--Users-m-a-peralta--claude-skills\memory\feedback_single_source_for_geometry.md`

Content: "Chrome and layout coordinates have exactly one source of truth: chrome.yml. Pattern helpers, QC rules, and helper constants all *read* it, never *re-encode* it. Numeric coordinate literals outside chrome.yml are a contract violation enforced by `check_chrome_field_single_source`. See F1 cross-skill placeholder contract pattern for the parallel idiom."

**Amend:** `feedback_existence_vs_content.md` — append a canonical example:

> Sidecar exists ≠ sidecar matches current template. `chrome.yml` carries a `source_template_sha8` freshness field; `finalize_deck.py` hard-fails on SHA mismatch. Test that the SHA check actually catches a mutated template (not just that the field exists).

**Edit:** `memory/MEMORY.md` — index both new files.

---

## 4. Phase 2 — deferred to v0.2.x

The original Phase 2 plan (layouts.yml with per-layout body_zone scaling, refactor all 14 patterns to compose inside body_zone via affine transform) is **substantially deferred**. Reason: Mario's templates (FedEx, Accenture, NFL) have large blank body zones that fit Slide Lab's canonical 1280×720 canvas; Phase 1's body-canonical class handles 90% of body slides without needing body_zone scaling.

If/when a template with a cramped body zone shows up (some financial-services templates), Phase 2 lands:

- `<stem>.layouts.yml` with per-layout `body_zone` rect for non-canonical body layouts
- Pattern helpers gain `body_zone: Rect` parameter
- `check_body_zone_purity` AST-check (no canonical-canvas integer literals in pattern helpers)
- Affine scaling from canonical 1240×540 reference to actual body_zone

Estimated effort if needed: ~3-5 days post-Phase-1.

---

## 5. Concrete artifacts

### 5.1 Fail-loud error format (build_deck.py exit code 9)

```
[1/5] Loading brief and template...
ERROR: brief is missing required `Layout:` field on these slides, and no
       `default_layout:` is set in the front-matter:

   Slide 1 — "OTC reboot: the case for action"
   Slide 4 — "Value framework — three workstreams"
   Slide 7 — "30-day sprint master plan"

Available layouts registered for FDX Template (from FDX Template.chrome.yml):
   cover_light           cover_dark
   body_canonical_light  body_canonical_dark
   section_divider       section_divider_dark
   table_full            chart_full
   appendix

Two ways to fix:

   (A) Add `default_layout: <name>` to the brief's YAML front-matter to apply
       one layout to every slide that doesn't override:

       ---
       client_template: C:\...\FDX Template.pptx
       deck_type: Recommendation / POV
       default_layout: body_canonical_light    <-- ADD THIS LINE
       ---

   (B) Add `**Layout:** <name>` to each slide block listed above.

Exit code: 9 (brief-missing-required-layout)
```

### 5.2 Cover-only layouts have `has_page_number: false`

`add_footer()` reads `chrome.has_page_number`. False → page number not drawn. Replaces the existing `page_type == "cover"` exemption in `slide-qc`'s footer-present check. Migrates one source of truth: layout-driven, not archetype-driven.

### 5.3 Existing briefs migration

The 2 existing FedEx-engagement briefs (`2026-05-26 OTC Internal Alignment\_session\narrative-brief-*.md` + `2026-05-26 Pre-Workshop Scope Alignment\_session\narrative-brief-*.md`) need either:

1. **Backfill:** add `default_layout: body_canonical_light` to front-matter + per-slide `**Layout:** cover_light` for slide 1 of each.
2. **Migrator script:** `scripts/migrate_brief_to_v3.py` that does (1) automatically. ~30 lines.

Migrator preferred. Lands as part of P1.4.

---

## 6. Test plan

### 6.1 Unit-level (per commit)

- `test_chrome_schema_roundtrip` — synthetic ChromeSpec → dict → revalidate.
- `test_chrome_extractor_classification` — synthetic 4-layout PPTX (2 body-canonical, 2 bespoke); assert each classified correctly.
- `test_text_role_color_flip` — call `add_title_block` with `text_role: light_on_dark`; PIL-inspect rendered text color is white (≥240 in R/G/B). Behavior check, not "field exists."

### 6.2 Integration smoke (end of P1.5)

Build a 4-slide synthetic brief (cover_dark, body_canonical_light, body_canonical_dark, section_divider) against a synthetic 6-layout PPTX. Run the full pipeline (build_deck → workers → finalize → compile). Open the final deck in PowerPoint, assert visually:

- Title bottom-y is identical between body_canonical_light and body_canonical_dark.
- Footer text doesn't collide with master chrome bands.
- Cover_dark has white title text at the cover_dark layout's title position (not body-canonical position).

### 6.3 Cleanup-chat-cannot-self-declare protocol

**This planning chat cannot run any of the above tests.** Even if the implementing chat runs them and reports green, that's `claimed-complete-by-implementation`. Only a fresh-context audit chat (no implementation narrative bias) can mark `audit-confirmed`. The audit chat must:

- Read this design lock cold.
- Read the implemented code without seeing the implementing chat's transcripts.
- Run the smoke test (6.2) independently.
- Read the contract test output and verify all 5 new checks actually fire when their failure conditions are planted.

---

## 7. Predictions to falsify (post-ship bets)

These are Agents D + R's predictions on the first real bugs. Treat as a falsifiable scorecard the audit chat checks against:

- **D's bet:** Within 2 builds after P1.5 ships, footer drifts 18-40 EMU because chrome extraction picks the wrong layout's footer placeholder when multiple match. **Catcher:** `check_layout_inheritance_roundtrip_per_layout` (per-layout, not per-template). If it caught the bug, mark prediction confirmed. If a different bug surfaced first, log what + why.
- **R's bet:** First brief built against the old format → slides 2-4 silently use `default_layout` but slide 1 cover ALSO renders at default geometry, losing the full-bleed cover background. **Catcher:** § 5.1 fail-loud + § 5.3 migrator. If both ship, this bug should be impossible. If it happens anyway, one of those didn't actually land.

---

## 8. Time estimate

| Phase | Estimate |
|---|---|
| Phase 1 (P1.1 - P1.6) | **~2.5-3 working days** |
| Phase 2 (deferred unless cramped template appears) | ~3-5 working days when needed |

Phase 1 includes the 5 contract checks (no separate "test-infra phase"), the 3 memory files (no separate "doc phase"), and the dark-variant `text_role` mechanism.

---

## 9. Mario's confirmations on file

1. **Body-canonical / bespoke classification:** ✅ confirmed.
2. **Don't modify source `.pptx`:** ✅ confirmed.
3. **`text_role` field for dark variants:** ✅ confirmed (preview at `C:\Users\m.a.peralta\Documents\slide-lab-dark-variant-preview-2026-05-28.pptx`).
4. **min_body_zone defensive rule lands with code:** ✅ confirmed.
5. **Fail-loud for `layout:` missing (option b with `default_layout:` escape hatch):** ✅ confirmed.
6. **Bottom-anchor invariant wins:** ✅ confirmed (will empirically validate at implementation).
7. **Hard SKELETON_REJECTED for min_body_zone:** ✅ confirmed.

---

## 10. Status flag — do not change without a fresh audit chat

Current: `planning-locked` (this chat).

Next states:
- `implementation-in-progress` — set by the implementing chat when work starts.
- `claimed-complete-by-implementation` — set by the implementing chat after green contract test + smoke test.
- `audit-confirmed` — set ONLY by a fresh-context audit chat after independent verification per § 6.3.

If a chat tries to skip a state, it's violating `feedback_cleanup_chat_cannot_self_declare.md`.
